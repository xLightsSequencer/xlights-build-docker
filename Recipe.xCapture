#!/bin/bash
export BASEDIR=/xCapture
mkdir ${BASEDIR}
cd ${BASEDIR}
git clone https://github.com/xLightsSequencer/xCapture.git --recurse-submodules xcapture-git
cd ${BASEDIR}/xcapture-git
git pull
colormake-short -j`nproc`
