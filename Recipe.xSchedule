#!/bin/bash
export BASEDIR=/xSchedule
mkdir ${BASEDIR}
cd ${BASEDIR}
git clone https://github.com/xLightsSequencer/xSchedule.git --recurse-submodules xschedule-git
cd ${BASEDIR}/xschedule-git
git pull
colormake-short -j`nproc`
